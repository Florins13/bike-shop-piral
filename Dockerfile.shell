FROM nginx:alpine

# Copy pre-built release files
COPY dist/release/ /usr/share/nginx/html/

# Custom nginx config for SPA routing
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
